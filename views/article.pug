extends layout

block content
  .container
    h1 #{article.title}
    h3.post-title #{article.subtitle}
    p.post-meta
      | Posted by #{article.author}
      | on #{moment(article.created_at).format('MMMM Do YYYY')}
    p.post-body
      | #{article.body}
